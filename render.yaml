services:
  - type: web
    name: pubnet
    runtime: python
    buildCommand: pip install .
    startCommand: gunicorn pubnet.gui.app:server -b 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
